<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!-- Temporary suppression: Drawable PNG assets slated for removal or vector replacement.
         Tooling constraints prevented binary relocation/deletion in this session.
         We ignore IconLocation for now to avoid noise while assets are deprecated.
         Follow-up: replace needed assets with vector or move to density folders; delete the rest. -->
    <issue id="IconLocation" severity="ignore" />
</lint>
